name: Check typescript

on:
  pull_request:
    branches: [main]

jobs:
  build-and-check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Setup Node
      - uses: actions/setup-node@v3
      - run: npm ci
      - run: npm run build
